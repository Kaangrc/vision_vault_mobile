import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Core Brand Colors
  static const Color primaryNavy = Color(0xFF0A192F);
  static const Color primaryObsidian = Color(0xFF0B0F19);

  // Accents
  static const Color accentTeal = Color(0xFF00E676); // Vibrant Teal
  static const Color accentCyan = Color(0xFF00E5FF);

  // Surface Colors
  static const Color surfaceLight = Color(0xFFF8F9FA); // Crisp off-white
  static const Color surfaceDark = Color(0xFF112240); // Midnight Blue surface

  // Text Colors
  static const Color textSlate = Color(0xFF8892B0);
  static const Color textLight = Color(0xFFCCD6F6);
  static const Color textDark = Color(0xFF0A192F);

  static const Color errorRed = Color(0xFFFF5252);
}
